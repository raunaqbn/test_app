<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "DawaKhana", root_path, class: "navbar-brand", id: "logo" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to "Services", root_path, id: "options"%></li>
        <li><%= link_to "Testimonials", testimonials_path, id: "options"%></li>
        <% if logged_in? %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false" id="options">Options <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to "Write Testimonial", new_testimonial_path %></li>
            <li><%= link_to "View Testimonials", testimonials_path%></li>
            <li><%= link_to "List Patients", users_path %></li>
            <li role="separator" class="divider"></li>
            <li><a href="#">Schedule Appointment</a></li>
            <li role="separator" class="divider"></li>
            <li><a href="#">View Records</a></li>
          </ul>
        </li>
        <% end %>
      </ul>
      <form class="navbar-form navbar-left" role="search">
        <div class="form-group">
          <input type="text" class="form-control" placeholder="Search">
        </div>
        <button type="submit" class="btn btn-default">Submit</button>
      </form>
      <ul class="nav navbar-nav navbar-right">
        <% if logged_in? %>
        <li><%= link_to current_user.username, user_path(current_user),id: "options" %></li>
        <% end %>
        <% if logged_in? %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false" id="options"><span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to "Update profile", edit_user_path(current_user) %></li>
            <li><%= link_to "Write Testimonial", new_testimonial_path %></li>
            <li><%= link_to "View Testimonials",  user_path(current_user)%></li>
            <li role="separator" class="divider"></li>
            <li><%= link_to 'Log out', logout_path, method: :delete %></li>
          </ul>
        </li>
        <% else%>
            <li><%= link_to 'Log in', login_path, id: "options" %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>